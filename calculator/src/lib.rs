pub mod sim_calculator{
    pub fn calculate1(no1:u32,no2:u32)
      {
    let mut add= no1 + no2;
println!("Add numbers : {}",add);
let mut mul= no1 * no2;
println!("Add numbers : {}",mul);
let mut div= no1 / no2;
println!("Add numbers : {}",div);
      }
    pub fn calculator2(no1:u32,no2:u32)
    {
        let mut sub= no2-no1;
        println!("Sutract :{}",sub )
    }
}